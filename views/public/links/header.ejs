<!doctype html>
<html lang="en">
  <head>
    <title>Title</title>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <!-- Bootstrap CSS -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">  
   
    <style>
        :root {
            --primary-color: #2196F3;
            --background-dark: #121212;
            --background-light: #1E1E1E;
            --text-primary: #FFFFFF;
            --text-secondary: #B0B0B0;
            --hover-color: #2C2C2C;
        }
    
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            background-color: var(--background-dark);
            color: var(--text-primary);
        }
    
        .container {
            display: flex;
            height: 100vh;
        }
    
        .sidebar {
            width: 220px;
            background-color: var(--background-light);
            padding: 20px;
            transition: all 0.3s ease;
            box-shadow: 2px 0 5px rgba(0,0,0,0.1);
        }
    
        .sidebar h2 {
            margin-top: 0;
            color: var(--primary-color);
            font-size: 1.5em;
            margin-bottom: 20px;
        }
    
        .sidebar ul {
            list-style-type: none;
            padding: 0;
        }
    
        .sidebar li {
            padding: 12px 15px;
            cursor: pointer;
            transition: background-color 0.3s ease, color 0.3s ease;
            border-radius: 5px;
            margin-bottom: 5px;
        }
    
        .sidebar li:hover, .sidebar li.active {
            background-color: var(--primary-color);
            color: var(--text-primary);
        }
    
        .main-content {
            flex-grow: 1;
            padding: 30px;
            background-color: var(--background-dark);
        }
    
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 100px;
            margin-bottom: 30px;
        }
    
        .header h1 {
            font-size: 2em;
            font-weight: 500;
        }
    
        .search-bar {
            width: 100%;
            padding: 12px;
            border: none;
            border-radius: 5px;
            background-color: var(--background-light);
            color: var(--text-primary);
            margin-bottom: 20px;
            transition: box-shadow 0.3s ease;
        }
    
        .search-bar:focus {
            outline: none;
            box-shadow: 0 0 0 2px var(--primary-color);
        }
    
        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0 10px;
        }
    
        th, td {
            text-align: left;
            padding: 15px;
            background-color: var(--background-light);
        }
    
        th {
            background-color: var(--primary-color);
            color: var(--text-primary);
            font-weight: 500;
        }
    
        tr {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
    
        tr:hover {
            transform: translateY(-3px);
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        }
    
        .amount-positive {
            color: #4CAF50;
        }
    
        .amount-negative {
            color: #F44336;
        }
    
        select {
            padding: 8px 12px;
            border-radius: 5px;
            background-color: var(--background-light);
            color: var(--text-primary);
            border: 1px solid var(--primary-color);
        }
    
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    
        .fade-in {
            animation: fadeIn 0.5s ease-out forwards;
        }
    </style>

</head>
  <body>
      <!-- <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <nav class="navbar navbar-expand-sm navbar-light bg-light">
                    <div class="sidebar fade-in">
                    <a class="navbar-brand" href="#">Welcome</a>
                    <a class="navbar-brand" href="#"><%= name %></a>
                    </div>
                     <button class="navbar-toggler d-lg-none" type="button" data-toggle="collapse" data-target="#collapsibleNavId" aria-controls="collapsibleNavId"
                        aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="collapsibleNavId">
                        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                            
                        </ul>
                        <form class="form-inline my-2 my-lg-0">
                            <ul class="navbar-nav">
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="dropdownId" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Action</a>
                                <div class="dropdown-menu" aria-labelledby="dropdownId" style="margin-left:-70px;">
                                    <a class="dropdown-item" href="#">Change Password</a>
                                    <a class="dropdown-item" href="/logout">Logout</a>
                                </div>
                            </li>
                        </ul> -->
                        <!-- <div class="main-content fade-in">
                            <div class="header">
                                <h1>Money</h1>
                                <div class="dropdown-menu" aria-labelledby="dropdownId" style="margin-left:-70px;">
                                    <a class="dropdown-item" href="#">Change Password</a>
                                    <a class="dropdown-item" href="/logout">Logout</a>
                                </div>
                            </div>
                            <input type="text" class="search-bar" placeholder="Search...">
                       </form>
                    </div> 
                </nav>
            </div>
        </div> -->

        <div class="main-content fade-in">
            <div class="header" style="background-color: var(--hover-color); border-radius: 10px;">
                <h1 style="margin-left: 225px;">Welcome <a href="#"><%= name %></a></h1>
                <!-- <div class="dropdown-menu" aria-labelledby="dropdownId" style="margin-left:-70px; color: white;">
                    <a class="dropdown-item" href="#">Change Password</a>
                    <a class="dropdown-item" href="/logout">Logout</a>
                </div> -->
                <button type="button" class="btn btn-warning" style="margin-right: -1332px;">Change Password</button>
                <br><br>
                <a href="/logout" style="margin-right: 50px;"><button type="button" class="btn btn-danger">Logout</button></a>
               
                
                <script>  document.querySelector('.search-bar').addEventListener('input', function(e) {             const searchTerm = e.target.value.toLowerCase();             const rows = document.querySelectorAll('#transactionBody tr');             rows.forEach(row => {                 const text = row.textContent.toLowerCase();                 row.style.display = text.includes(searchTerm) ? '' : 'none';             });         });       </script>
            </div>